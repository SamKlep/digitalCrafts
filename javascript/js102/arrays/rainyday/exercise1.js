function unique(array){
    var object= {};
    
    for (var i=0; i<array.length; i++){
        var currentItem=array[i];
        object[currentItems]= "blah";
    }

    var uniqueItems = object.keys();
    return uniqueItems;
    }
} 
 
