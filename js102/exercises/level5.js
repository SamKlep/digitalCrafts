function lvl5exercise1 () {
  // Push the string "hello" into the array and return it.
  let array = [1, 'adam'];
  let str1 = "hello";
  array.push(str1);
  return array;
}

function lvl5exercise2 () {
  // Remove the last element from the array and return it
  var array = [1, 'adam'];
  array.pop();
  return array;
}

function lvl5exercise3 () {
  // Return the length of the array
  var array = [1, 'adam'];
  array.length;
  return array;
}

function lvl5exercise4 () {
  // Return the index of "adam" in the array
  var arr = [1, 'adam'];
  let indexWorld = arr.indexOf('world');
return arr;
}
